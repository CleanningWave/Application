import Header from "@/components/Header";
import { Text, View } from "react-native";

const ResultLayout = () => {
  return (
    <View>
      <Header title={"AI 분석 결과"} />
      <Text>ResultLayout</Text>
    </View>
  );
};

export default ResultLayout;
